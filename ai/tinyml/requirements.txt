# Host-only: nothing here is cross-compiled or flashed. Python 3.12 on macOS
# arm64 is what these were run on.
#
#     python3.12 -m venv ai/tinyml/.venv
#     ai/tinyml/.venv/bin/pip install -r ai/tinyml/requirements.txt
#
# tflite-micro is deliberately absent. Its wheels are cp310/cp311 and
# manylinux_2_28_x86_64 only, so the bit-exact TFLM reference-kernel oracle
# cannot run on an arm64 Mac and would have to run in CI on Linux. RESULTS.md
# explains why that is moot while a decision tree is the chosen runtime.
tensorflow==2.21.0
scikit-learn==1.9.0
emlearn==0.23.2
numpy==2.5.1
pandas==3.0.5
scipy==1.18.0
