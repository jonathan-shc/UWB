# Keeps the SHA-pinned GitHub Actions current (Dependabot updates the pin and
# its version comment together).
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
