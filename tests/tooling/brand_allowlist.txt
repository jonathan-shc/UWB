# Every spelling of "aliro" this tree is allowed to contain, as extended regular
# expressions matched against whole lines. Anything else is a leftover from the
# rename and fails check-purity.
#
# The rule the list encodes: we do not own the word. It stays only where it
# names something outside this repository -- a Nordic add-on symbol, a Matter
# SDK entry point, a CSA document, an Apple profile, or bytes on the wire.
#
# Adding a line here is a claim that the name belongs to someone else. If the
# thing is ours, rename it instead.

# --- Nordic door-lock add-on: C++ API we link against -------------------------
namespace Aliro
using namespace Aliro
Aliro::
AliroError
AliroErrorCode
AliroStack
AliroWorkqueueSubmit
include <aliro
<aliro/
includes aliro/
<aliro_workqueue/
# The add-on's error and status codes, named one by one. This was written as
# ALIRO_[A-Z0-9_]+ and that wildcard excused our own names too: ALIRO_BUILD_ROOT
# was one of ours and sat here unrenamed through the entire rebrand because the
# allowlist said it belonged to Nordic. A list you have to extend deliberately is
# the point -- a pattern that matches whatever appears next is not a decision.
ALIRO_APDU_STATUS_INVALID
ALIRO_DECRYPTION_COUNTER_OVERFLOW
ALIRO_ENCRYPTION_COUNTER_OVERFLOW
ALIRO_ERROR_INTERNAL
ALIRO_ERROR_MAX
ALIRO_ERROR_NOT_IMPLEMENTED
ALIRO_ERROR_UNKNOWN
ALIRO_INVALID_ARGUMENT
ALIRO_INVALID_AUTHENTICATION_TAG
ALIRO_INVALID_DATA_CONTENT
ALIRO_INVALID_DATA_FORMAT
ALIRO_INVALID_SIGNATURE
ALIRO_INVALID_STATE
ALIRO_KEY_ALREADY_EXISTS
ALIRO_NO_ERROR
ALIRO_NO_MEMORY
ALIRO_NO_PUBLIC_KEY_IN_RESPONSE
ALIRO_PUBLIC_KEY_EXPIRED
ALIRO_PUBLIC_KEY_NOT_FOUND
ALIRO_PUBLIC_KEY_NOT_TRUSTED
ALIRO_SESSION_NOT_FOUND
ALIRO_SESSION_TERMINATE
ALIRO_TIMEOUT
ALIRO_TLV_BUFFER_TOO_SMALL
ALIRO_TLV_END_OF_TLV
ALIRO_TLV_INVALID_LEN
ALIRO_TLV_INVALID_TAG
ALIRO_TLV_WRONG_DATA_TYPE
ALIRO_VERSION_NOT_SUPPORTED
aliro_workqueue
aliroSupported[A-Za-z]*
Aliro(Reader|Credential|Endpoint|Group|Expedited|BLE)[A-Za-z]*

# --- Nordic add-on: upstream paths and Kconfig, quoted in our build files -----
subsys/aliro
applications/matter-aliro
matter-aliro-door-lock-app
CONFIG_NCS_ALIRO_[A-Z0-9_]+
CONFIG_DOOR_LOCK_ALIRO_[A-Z0-9_]+
libaliro
the Aliro add-on

# --- Matter SDK: DoorLock cluster surface we implement or call ----------------
kAliro[A-Za-z]*
(Get|Set|Clear)[A-Za-z]*Aliro[A-Za-z]*
UserIndexForAliroCredential
AliroReaderVerificationKey
Aliro credential types \(DoorLock/Enums.h
NumberOfAliro[A-Za-z]*
# Our own constants for DoorLock cluster entities the Matter Data Model itself
# names Aliro -- credential types, attribute and command ids, feature bits, and
# the reader-config TLV tags that carry them. Renaming these would make our
# spelling disagree with the specification they mirror, which is the same reason
# the kAliro* enums above and the aliro-a/aliro-r wire docTypes below stay.
MATTER_[A-Z0-9_]*ALIRO[A-Z0-9_]*
TAG_ALIRO_CFG_[A-Z0-9_]+
# The add-on's own Kconfig symbol, quoted without its CONFIG_ prefix.
NCS_ALIRO_BLE_UWB

# --- esp_matter: door-lock feature helpers ------------------------------------
aliro_provisioning
aliro_bleuwb
Aliro(Provisioning|BLEUWB)
UwbAliroFlow

# --- CSA documents, cited by section or table --------------------------------
Aliro Lab
Aliro Specification
Aliro Table
Aliro §

# --- Apple's ECP profile name ------------------------------------------------
Aliro \(Unified Access\)

# --- The add-on's own UWB facade, named as theirs -----------------------------
add-on Aliro UWB facade
@brief Aliro UWB facade

# --- Wire bytes: CBOR docTypes from the spec, not names we chose --------------
aliro-[ar]

# --- The add-on's build-system names ------------------------------------------
# Upstream's imported CMake target is called `aliro`, and lib/aliro/CMakeLists.txt
# gates the prebuilt blob on if(NOT TARGET aliro). The source stack has to spell
# it exactly to displace it from ZEPHYR_LIBS.
#
# Every other entry in this file is protected by something about the token: a
# namespace, a case convention, an include delimiter. A bare CMake identifier
# has none of those, so the rename reached it, and list(REMOVE_ITEM) removed
# nothing and said nothing. Named here one line at a time for the same reason
# the ALIRO_ codes above are.
`aliro`
lib/aliro/CMakeLists\.txt
"aliro" IN_LIST
REMOVE_ITEM _zephyr_libraries aliro

# The one add-on include we reach by quoted path rather than angle brackets.
# <aliro/ above covers the other thirteen; it is spelled with the delimiter, so
# it never covered this one, which is how the rename got it.
"aliro/platform/

# --- The CSA specification, named as the CSA's ---------------------------------
# The README says which standard this firmware implements. That is the word
# naming a CSA document, the same class as the citations above, and not a label
# for any code here. Kept to this one spelling on purpose: "implements Aliro"
# describes the specification, where "the Aliro reader" would be claiming the
# name for ours.
[Ii]mplements Aliro, the CSA's door-lock credential standard

# --- The guides under docs/, citing the standard and the add-on ---------------
# The bring-up and porting guides have to name the specification they are
# implementing and the vendor code they sit on, the same way the source
# comments above do. Every pattern here names a CSA document, a CSA-defined
# protocol element, an Apple Wallet credential, the Nordic add-on, or Qorvo's
# reference application. None of them labels code in this repository: our own
# identifiers in those guides were renamed rather than excused, which is what
# took the count from 1,176 lines to these.
Aliro 1\.0
Aliro Specification
[Tt]he Aliro Door Lock cluster
Aliro (BLE|UWB) (advertisement|transaction|door-lock|credential)
Aliro (service|advertising) (data|support|with)
Aliro key (in|provisioned in) Wallet
Aliro-capable iPhone
Home/Aliro UWB credential
Aliro/Home UWB credential
an Aliro lock
Aliro work-queue stack
Enable Aliro over BLE \+ UWB
Qorvo Aliro reference application
introducing-aliro-1-0-a-unified-standard
# The specification's own wire and cluster vocabulary, as the guides cite it:
# the PDF by its CSA document number, the attribute block and message header it
# defines, the APDU exchange and transaction it names, and Espressif's own
# esp-aliro component.
Aliro attribute block
Aliro message header
Aliro APDU exchange
Aliro transaction
Aliro credential-auth
Aliro 0xFFF2 advertisement
Aliro ranging session-id
Aliro_1\.0_specification\.pdf
esp-aliro
Nordic Aliro library
Aliro key during Matter commissioning

# The add-on's C++ entry points, quoted in bring-up guides as log or API text.
AliroStart\(\)
AliroInit
Failed to start Aliro
legacy Nordic Aliro binary
app-side `src/aliro/`

# The Matter Data Model's own door-lock vocabulary, as the porting notes cite it.
(Matter )?Aliro cluster
BLE\+UWB Aliro flow

# The specification, named as the specification: what it mandates, what it
# defines on the wire, what it does not cover, and Apple's certification page.
Aliro support
Aliro features
Aliro `0xFFF2`
Aliro 0xFFF2 service
Aliro auth commands
Aliro-over-NFC
Aliro-certified platforms
Aliro's time-based credential
Aliro makes NFC the mandatory transport
does not mention Aliro
the published Aliro
It means Aliro
and Aliro only
route the Aliro
Full Aliro UWB
