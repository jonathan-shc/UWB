#ifndef TEST_MBEDTLS_ENTROPY_H
#define TEST_MBEDTLS_ENTROPY_H

/* The one error code the port's hardware poll returns. */
#define MBEDTLS_ERR_ENTROPY_SOURCE_FAILED -0x003C

#endif /* TEST_MBEDTLS_ENTROPY_H */
