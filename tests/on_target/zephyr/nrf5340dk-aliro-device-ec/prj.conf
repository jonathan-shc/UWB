# nRF5340 DK: Aliro device (initiator) EC self-test against the real
# nrf_security/CryptoCell PSA backend. printf goes to the DK's UART console.
CONFIG_MAIN_STACK_SIZE=16384
CONFIG_STDOUT_CONSOLE=y

CONFIG_NRF_SECURITY=y
CONFIG_MBEDTLS_PSA_CRYPTO_C=y

# PSA crypto (Oberon/CC3xx) allocates working buffers from the mbedTLS heap.
# Without it, psa_import_key / AES-GCM / P-256 fail with -141
# (PSA_ERROR_INSUFFICIENT_MEMORY). Sized for the P-256 ECDH/ECDSA loopback.
CONFIG_MBEDTLS_ENABLE_HEAP=y
CONFIG_MBEDTLS_HEAP_SIZE=32768

# The exact primitives ultrawidelock_prim_psa.c calls: P-256 keygen/import/export,
# ECDH raw key agreement, ECDSA(SHA-256) sign/verify, AES-256-GCM (secure
# channels + fast cryptogram), SHA-256 and the RNG.
CONFIG_PSA_WANT_ALG_ECDSA=y
CONFIG_PSA_WANT_ALG_ECDH=y
CONFIG_PSA_WANT_KEY_TYPE_ECC_KEY_PAIR_GENERATE=y
CONFIG_PSA_WANT_KEY_TYPE_ECC_KEY_PAIR_IMPORT=y
CONFIG_PSA_WANT_KEY_TYPE_ECC_KEY_PAIR_EXPORT=y
CONFIG_PSA_WANT_KEY_TYPE_ECC_KEY_PAIR_DERIVE=y
CONFIG_PSA_WANT_KEY_TYPE_ECC_PUBLIC_KEY=y
CONFIG_PSA_WANT_ECC_SECP_R1_256=y
CONFIG_PSA_WANT_ALG_SHA_256=y
CONFIG_PSA_WANT_ALG_GCM=y
CONFIG_PSA_WANT_KEY_TYPE_AES=y
CONFIG_PSA_WANT_GENERATE_RANDOM=y
