/* nfcfake: <zephyr/sys/atomic.h>. The definitions live in kernel.h so the two
 * cannot disagree; this exists because transport_pn532.cpp includes it. */
#ifndef NFCFAKE_ZEPHYR_SYS_ATOMIC_H
#define NFCFAKE_ZEPHYR_SYS_ATOMIC_H

#include <zephyr/kernel.h>

#endif /* NFCFAKE_ZEPHYR_SYS_ATOMIC_H */
