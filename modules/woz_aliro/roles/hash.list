# woz_aliro role: SHA-256/HMAC/HKDF core — its own role because the host KAT
# suite compiles it without the rest of the crypto role. Consumers: firmware/,
# ports/nrf5340dk/{initiator,on_target_ec}, ports/esp32/components/aliro_crypto,
# tests/host/sources.sh. One repo-root-relative path per line, sorted; add HERE only.
modules/woz_aliro/src/aliro_hash.c
