# ultrawidelock_cred role: APDU codec + 4-byte L2CAP envelope, shared by BOTH ends of
# the Access Protocol. Consumers: the reader app, the Zephyr initiator and
# on-target test, and ports/esp32/components/{ultrawidelock_reader,ultrawidelock_device}.
# One repo-root-relative path per line, sorted; add HERE and nowhere else.
modules/ultrawidelock_cred/src/ultrawidelock_apdu.c
