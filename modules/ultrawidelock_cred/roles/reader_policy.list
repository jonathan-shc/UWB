# ultrawidelock_cred role: reader-side stores + gating policy (trust store, RSSI gate,
# approach controller) — split from reader.list because the host KAT suite
# compiles these without the engine. Consumers: apps/dwm3001cdk-lock/, ports/esp32/
# components/ultrawidelock_reader, tests/host/sources.sh. Sorted; add HERE only.
modules/ultrawidelock_cred/src/ultrawidelock_approach.c
modules/ultrawidelock_cred/src/ultrawidelock_prov.c
modules/ultrawidelock_cred/src/ultrawidelock_rssi_gate.c
