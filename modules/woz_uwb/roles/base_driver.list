# woz_uwb tier: base radio driver (CONFIG_WOZ_UWB) — needs a DW3000 behind it,
# so the host suite does not compile it. Consumers: modules/woz_uwb/CMakeLists.txt,
# ports/esp32/components/woz_uwb.
# One repo-root-relative path per line, sorted; add HERE and nowhere else.
modules/woz_uwb/src/driver/uwb_min.c
