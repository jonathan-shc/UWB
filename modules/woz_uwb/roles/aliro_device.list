# woz_uwb tier: device-side (initiator) M1/M3 parser + M2/M4 builder — never in
# the woz_uwb Zephyr module (that builds the reader). Consumers:
# ports/nrf5340dk/initiator, tests/host/sources.sh (tested against the real
# reader codec). One repo-root-relative path per line, sorted; add HERE only.
modules/woz_uwb/src/aliro/aliro_device_uwb.c
