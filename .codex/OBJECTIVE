Implement a complete FreeRTOS port of dwm3001cdk-lock

Done: assessed feasibility, saved the staged plan, created the sibling application and standalone nRF52833 FreeRTOS port trees, implemented the timing, allocation, mutex, logging, work, semaphore, init-hook, static-thread, serialized OpenThread runtime, static NimBLE/SDC HCI transport, shared MPSL worker, fixed peripheral map, nRF 802.15.4 IRQ/random/temperature contracts, and the MPSL/SoftDevice Controller startup sequencer that claims the frozen interrupt priorities, links only the peripheral GATT/CoC feature set, sizes the static controller pool, registers the entropy source, and publishes the HCI transport contract; pinned the Qorvo/OpenThread/Nordic/NimBLE source set, and added host, source, and port-purity gates.
Remains: port the pinned SDC opcode dispatcher away from Zephyr types and feed it to woz_freertos_radio_start(), initialize the NimBLE host and its FreeRTOS NPL, implement the nRF 802.15.4 RTC2/clock and OpenThread radio platform, prove nRF52833 BLE/OpenThread coexistence, then implement and validate the board, UWB, storage, crypto, USB, boot, and DFU backends plus product orchestration.
Gates: preserve the hardware-in-loop Aliro BLE/URSK/Pre-POLL verdict, meet the DW3110 response-arm timing under concurrent BLE and Thread load, fit 512 KB flash and 128 KB RAM, and retain recoverable signed update behavior.
