{
  "name": "@openaliro/activity",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Discord Activity shell that serves web-twin/ unmodified",
  "scripts": {
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "lock": "node scripts/write-lock.mjs",
    "deploy": "vite build && WRANGLER_SEND_METRICS=false wrangler pages deploy dist --project-name openaliro-twin --branch main --commit-hash \"\" --commit-message \"activity build\" --commit-dirty true",
    "verify-deploy": "node scripts/verify-deploy.mjs",
    "test": "node scripts/participants-test.mjs"
  },
  "dependencies": {
    "@discord/embedded-app-sdk": "2.5.0"
  },
  "devDependencies": {
    "@types/node": "26.1.2",
    "typescript": "7.0.2",
    "vite": "8.2.0",
    "wrangler": "4.118.0"
  },
  "overrides": {
    "undici": "7.29.0"
  }
}
