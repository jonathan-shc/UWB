/*
 * The Zephyr-shaped macros the pinned opcode dispatcher needs. They are shared
 * with the OpenThread radio platform's compat layer, so the definitions live
 * in the port's own include directory rather than here.
 */
#ifndef WOZ_HCI_COMPAT_UTIL_H
#define WOZ_HCI_COMPAT_UTIL_H

#include <woz_freertos_vendor_macros.h>

#endif /* WOZ_HCI_COMPAT_UTIL_H */
