/* SPDX-License-Identifier: ISC */

/*
 * Zephyr-shaped entry point for the kernel objects the pinned OpenThread radio
 * platform uses. The implementations are the port's own; see
 * ultrawidelock_freertos_ot_kernel.h for what is provided and why it is only this much.
 */
#ifndef ULTRAWIDELOCK_OT_COMPAT_ZEPHYR_KERNEL_H
#define ULTRAWIDELOCK_OT_COMPAT_ZEPHYR_KERNEL_H

#include "ultrawidelock_freertos_ot_kernel.h"

#endif /* ULTRAWIDELOCK_OT_COMPAT_ZEPHYR_KERNEL_H */
