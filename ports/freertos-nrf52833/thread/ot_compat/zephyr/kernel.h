/*
 * Zephyr-shaped entry point for the kernel objects the pinned OpenThread radio
 * platform uses. The implementations are the port's own; see
 * woz_freertos_ot_kernel.h for what is provided and why it is only this much.
 */
#ifndef WOZ_OT_COMPAT_ZEPHYR_KERNEL_H
#define WOZ_OT_COMPAT_ZEPHYR_KERNEL_H

#include "woz_freertos_ot_kernel.h"

#endif /* WOZ_OT_COMPAT_ZEPHYR_KERNEL_H */
