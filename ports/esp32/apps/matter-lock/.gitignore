# ESP-IDF / esp-matter build output + generated / fetched artifacts.
# sdkconfig.defaults* are the tracked source of truth.
/build/
/sdkconfig
/sdkconfig.old
# `make lab` / `make term LOG=` capture logs + their rendered reports. Bench
# artifacts: a capture worth keeping belongs in tests/host/data/, named.
/*.log
/*.log.html
/dependencies.lock
/managed_components/
