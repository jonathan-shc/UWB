# ESP-IDF / esp-matter build output + generated / fetched artifacts.
# sdkconfig.defaults* and sdkconfig.lab are the tracked source of truth.
/build/
/build-lab/
/sdkconfig
/sdkconfig.old
/dependencies.lock
/managed_components/
